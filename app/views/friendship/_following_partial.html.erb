<% if !Friendship.following?(current_user, user)%>
<%=link_to "Follow", {:controller => 'friendship', :action => 'follow_user', :id => user.id}, :remote => true%>
<%else%>
<%=link_to "Unfollow", {:controller => 'friendship', :action=> 'unfollow_user', :id => user.id}, :remote => true%>
<%end%>
